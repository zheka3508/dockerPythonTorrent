# Telegram Bot для управления Transmission

Telegram бот для управления загрузками торрентов через Transmission, установленный на роутере Keenetic.

## Возможности

- 📥 **Автоматическая загрузка**: Отправьте .torrent файл боту, и он автоматически начнет загрузку
- 📋 **Просмотр загрузок**: Просмотр всех загрузок или только активных
- ⏸️ **Управление**: Поставить на паузу или продолжить все загрузки
- 📊 **Детальная информация**: Для каждой загрузки отображается:
  - Статус (загружается, раздается, остановлен и т.д.)
  - Процент загрузки
  - Загружено / Всего
  - Скорость загрузки и отдачи
  - Количество пиров (всего, отдаем, получаем)

## Требования

- Docker и Docker Compose
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- Transmission с включенным RPC (на роутере Keenetic)

## Установка и запуск

### 1. Получите токен бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен

### 2. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TRANSMISSION_HOST=192.168.1.1
TRANSMISSION_PORT=9091
TRANSMISSION_USERNAME=torr
TRANSMISSION_PASSWORD=h3YTeVcPfyx5NXH
TRANSMISSION_PATH=/transmission/rpc
```

### 3. Запустите бота

```bash
docker-compose up -d
```

Для просмотра логов:

```bash
docker-compose logs -f
```

### 4. Остановка бота

```bash
docker-compose down
```

## Использование

После запуска бота, найдите его в Telegram и отправьте команду `/start`.

### Доступные команды:

- `/start` - Показать приветственное сообщение
- `/help` - Показать справку
- `/all` - Показать все загрузки (включая завершенные)
- `/active` - Показать только активные загрузки
- `/pause` - Поставить все загрузки на паузу
- `/resume` - Продолжить все загрузки

### Загрузка торрентов:

Просто отправьте `.torrent` файл боту, и он автоматически начнет загрузку в Transmission.

## Настройка Transmission на Keenetic

Убедитесь, что в настройках Transmission на роутере:

1. Включен RPC (Remote Procedure Call)
2. RPC доступен по адресу `http://192.168.1.1:9091/transmission/rpc`
3. Установлены правильные логин и пароль
4. RPC доступен из локальной сети (не только localhost)

## Структура проекта

```
.
├── bot.py                  # Основной файл бота
├── transmission_client.py  # Клиент для работы с Transmission
├── requirements.txt        # Зависимости Python
├── Dockerfile             # Docker образ
├── docker-compose.yml     # Docker Compose конфигурация
└── README.md             # Документация
```

## Технологии

- Python 3.14
- python-telegram-bot 22.5
- transmission-rpc 5.0.0
- Docker

## Устранение неполадок

### Бот не отвечает

1. Проверьте, что контейнер запущен: `docker-compose ps`
2. Проверьте логи: `docker-compose logs`
3. Убедитесь, что токен бота правильный

### Ошибка подключения к Transmission

1. Проверьте, что Transmission запущен на роутере
2. Убедитесь, что RPC включен и доступен
3. Проверьте правильность адреса, порта, логина и пароля в `.env`
4. Убедитесь, что контейнер может достичь роутер (проверьте сетевые настройки Docker)

### Торрент не добавляется

1. Проверьте, что файл действительно является .torrent файлом
2. Проверьте логи на наличие ошибок
3. Убедитесь, что Transmission имеет права на запись в директорию загрузок

## Лицензия

MIT

